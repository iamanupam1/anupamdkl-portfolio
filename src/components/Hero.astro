<section class="hero" id="hero">
  <div class="hero-gradient"></div>
  <div class="hero-orb hero-orb-1"></div>
  <div class="hero-orb hero-orb-2"></div>
  <div class="hero-orb hero-orb-3"></div>
  <div class="hero-grid"></div>
  <div class="hero-particles" id="hero-particles"></div>
  <div class="hero-vignette"></div>

  <div class="hero-content">
    <div class="hero-label">Full-Stack Engineer</div>
    <h1 class="hero-title">
      <span class="hero-word">Building</span>
      <span class="hero-word">digital</span>
      <br />
      <span class="hero-word hero-accent">experiences</span>
    </h1>
    <div class="hero-divider"></div>
    <p class="hero-subtitle">Crafting code that feels alive</p>
  </div>

  <div class="scroll-indicator" id="scroll-indicator">
    <div class="scroll-line"></div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    overflow: hidden;
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 25% 35%, rgba(124, 106, 239, 0.25) 0%, transparent 55%),
      radial-gradient(ellipse at 75% 55%, rgba(167, 139, 250, 0.15) 0%, transparent 45%),
      radial-gradient(ellipse at 50% 90%, rgba(232, 121, 168, 0.1) 0%, transparent 40%),
      radial-gradient(ellipse at 60% 20%, rgba(99, 102, 241, 0.1) 0%, transparent 50%);
    opacity: 0;
  }

  /* Floating ambient orbs */
  .hero-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
    animation: orbFloat 20s ease-in-out infinite;
  }

  .hero-orb-1 {
    width: 500px;
    height: 500px;
    background: rgba(124, 106, 239, 0.08);
    top: -10%;
    left: -5%;
    animation-delay: 0s;
  }

  .hero-orb-2 {
    width: 400px;
    height: 400px;
    background: rgba(232, 121, 168, 0.06);
    bottom: -10%;
    right: -5%;
    animation-delay: -7s;
  }

  .hero-orb-3 {
    width: 300px;
    height: 300px;
    background: rgba(167, 139, 250, 0.05);
    top: 40%;
    right: 20%;
    animation-delay: -14s;
  }

  @keyframes orbFloat {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(30px, -20px) scale(1.05); }
    50% { transform: translate(-20px, 15px) scale(0.95); }
    75% { transform: translate(15px, 25px) scale(1.02); }
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(124, 106, 239, 0.025) 1px, transparent 1px),
      linear-gradient(90deg, rgba(124, 106, 239, 0.025) 1px, transparent 1px);
    background-size: 80px 80px;
    pointer-events: none;
    mask-image: radial-gradient(ellipse at center, black 30%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse at center, black 30%, transparent 70%);
  }

  .hero-vignette {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: radial-gradient(ellipse at center, transparent 40%, var(--bg) 100%);
  }

  .hero-particles {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 10;
    text-align: center;
    padding: 0 24px;
  }

  .hero-label {
    font-family: var(--font-body);
    color: var(--text-dim);
    font-size: 11px;
    letter-spacing: 5px;
    text-transform: uppercase;
    margin-bottom: 20px;
    opacity: 0;
    font-weight: 300;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(44px, 8vw, 84px);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 28px;
    letter-spacing: -0.5px;
  }

  .hero-word {
    display: inline-block;
    opacity: 0;
  }

  .hero-accent {
    font-weight: 700;
    letter-spacing: 0px;
    background: linear-gradient(135deg, var(--accent-start), var(--accent-mid), var(--accent-end));
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-divider {
    width: 48px;
    height: 2px;
    background: linear-gradient(90deg, var(--accent-start), var(--accent-end));
    margin: 0 auto 28px;
    transform: scaleX(0);
    border-radius: 1px;
  }

  .hero-subtitle {
    font-family: var(--font-body);
    color: var(--text-dim);
    font-size: 15px;
    font-weight: 300;
    letter-spacing: 1.5px;
    opacity: 0;
  }

  /* Minimal scroll line instead of mouse icon */
  .scroll-indicator {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 10;
    opacity: 0;
  }

  .scroll-line {
    width: 1px;
    height: 48px;
    background: linear-gradient(to bottom, var(--accent-start), transparent);
    position: relative;
    overflow: hidden;
  }

  .scroll-line::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 12px;
    background: var(--accent-mid);
    animation: scrollPulse 2s ease-in-out infinite;
  }

  @keyframes scrollPulse {
    0% { top: -12px; opacity: 0; }
    30% { opacity: 1; }
    100% { top: 48px; opacity: 0; }
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: clamp(32px, 10vw, 56px);
    }
    .hero-orb { display: none; }
  }

  @keyframes particleFloat {
    0% {
      transform: translateY(100vh) scale(0);
      opacity: 0;
    }
    10% {
      opacity: 0.6;
    }
    90% {
      opacity: 0.6;
    }
    100% {
      transform: translateY(-10vh) scale(1);
      opacity: 0;
    }
  }
</style>
