<section class="hero" id="hero">
  <div class="hero-gradient">
    <div class="gradient-orb gradient-orb-1" id="gradient-orb-1"></div>
    <div class="gradient-orb gradient-orb-2" id="gradient-orb-2"></div>
    <div class="gradient-orb gradient-orb-3" id="gradient-orb-3"></div>
  </div>
  <canvas class="hero-dot-canvas" id="hero-dot-canvas"></canvas>
  <div class="hero-fade"></div>
  <div class="hero-noise"></div>

  <div class="hero-content" id="hero-content">
    <div class="hero-label" id="hero-label">Full-Stack Engineer</div>
    <h1 class="hero-title">
      <span class="hero-word" id="hero-line1">Building digital</span><br />
      <span class="hero-word hero-accent" id="hero-accent">experiences</span>
    </h1>
    <div class="hero-divider" id="hero-divider"></div>
    <p class="hero-subtitle" id="hero-subtitle">Crafting code that feels alive</p>
  </div>

  <div class="hero-content-reveal" id="hero-reveal">
    <div class="hero-label">Full-Stack Engineer</div>
    <h1 class="hero-title">
      <span class="hero-word">Building digital</span><br />
      <span class="hero-word hero-accent">experiences</span>
    </h1>
    <div class="hero-divider" style="transform: scaleX(1);"></div>
    <p class="hero-subtitle">Crafting code that feels alive</p>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    position: relative;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    --mx: 50%;
    --my: 50%;
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    will-change: transform;
    transition: transform 0.4s cubic-bezier(0.22, 1, 0.36, 1);
    animation: gradientShift 8s ease-in-out infinite alternate;
  }

  .gradient-orb-1 {
    width: 600px;
    height: 600px;
    background: var(--accent-glow-strong);
    top: 5%;
    left: 10%;
  }

  .gradient-orb-2 {
    width: 500px;
    height: 500px;
    background: var(--accent-glow);
    bottom: 0%;
    right: 5%;
    animation-delay: 2s;
  }

  .gradient-orb-3 {
    width: 400px;
    height: 400px;
    background: var(--accent-tint);
    top: 40%;
    left: 45%;
    animation-delay: 4s;
  }

  .hero-dot-canvas {
    position: absolute;
    inset: 0;
    z-index: 2;
    pointer-events: none;
  }

  .hero-fade {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 200px;
    background: linear-gradient(to bottom, transparent, var(--bg));
    z-index: 5;
    pointer-events: none;
  }

  .hero-noise {
    position: absolute;
    inset: 0;
    opacity: var(--noise-opacity);
    z-index: 3;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    background-size: 256px;
    pointer-events: none;
  }

  @keyframes gradientShift {
    0% { opacity: 0.8; transform: scale(1); }
    100% { opacity: 1; transform: scale(1.05); }
  }

  .hero-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    position: relative;
    z-index: 10;
    padding: 0 24px;
  }

  .hero-content-reveal {
    position: absolute;
    inset: 0;
    z-index: 11;
    pointer-events: none;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 0 24px;
    -webkit-mask-image: radial-gradient(circle 180px at var(--mx) var(--my), rgba(0,0,0,0.9) 0%, transparent 100%);
    mask-image: radial-gradient(circle 180px at var(--mx) var(--my), rgba(0,0,0,0.9) 0%, transparent 100%);
  }

  .hero-content-reveal .hero-label {
    color: var(--accent);
    opacity: 0.7;
  }

  .hero-content-reveal .hero-title {
    -webkit-text-stroke: 1.5px var(--accent);
    color: transparent;
    transform: translate(3px, -2px);
  }

  .hero-content-reveal .hero-accent {
    -webkit-text-stroke: 2px var(--accent);
    color: transparent;
    text-shadow: 0 0 30px var(--accent-glow-strong);
  }

  .hero-content-reveal .hero-divider {
    background: var(--accent);
    height: 2px;
    opacity: 0.8;
    width: 60px;
    margin-bottom: 24px;
  }

  .hero-content-reveal .hero-subtitle {
    color: var(--accent);
    opacity: 0.6;
  }

  /* Hide reveal layer and canvas on touch/mobile */
  @media (pointer: coarse) {
    .hero-content-reveal,
    .hero-dot-canvas {
      display: none;
    }
  }

  .hero-label {
    color: var(--text-dim);
    font-size: 12px;
    letter-spacing: 4px;
    text-transform: uppercase;
    margin-bottom: 16px;
    opacity: 0;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(40px, 8vw, 80px);
    font-weight: 400;
    line-height: 1.1;
    margin-bottom: 24px;
  }

  .hero-word {
    display: inline-block;
    opacity: 0;
  }

  .hero-accent {
    font-weight: 800;
    color: var(--accent);
  }

  .hero-divider {
    width: 60px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--accent), transparent);
    margin-bottom: 24px;
    transform: scaleX(0);
  }

  .hero-subtitle {
    color: var(--text-dim);
    font-size: 16px;
    font-weight: 300;
    letter-spacing: 0.5px;
    opacity: 0;
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: clamp(32px, 10vw, 56px);
    }
  }
</style>
